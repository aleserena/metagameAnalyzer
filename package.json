{
  "name": "mtgtop8scrapper",
  "private": true,
  "scripts": {
    "build": "cd web && npm run build",
    "test": "cd web && npm run test:run",
    "test:py": "python -m pytest",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
